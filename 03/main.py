from Calculo import Calculo

resultado = Calculo.calcular(5, 4)
print(resultado)

resultado = Calculo.calcular(3, 4, 5)
print(resultado)

resultado = Calculo.calcular(4, 3.0)
print(resultado)

resultado = Calculo.calcular(2.0)
print(resultado)

resultado = Calculo.calcular([0,0], [3,0], [2,5])
print(resultado)